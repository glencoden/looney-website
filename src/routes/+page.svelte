<video
    class="video"
    src='teaser-1v5.m4v'
    autoplay
    muted
    loop
>
</video>

<style>
    .video {
        z-index: var(--level-2);
        position: fixed;
        left: 50%;
        top: var(--nav-height);
        transform: translateX(-50%);
        width: var(--video-width);
    }
</style>